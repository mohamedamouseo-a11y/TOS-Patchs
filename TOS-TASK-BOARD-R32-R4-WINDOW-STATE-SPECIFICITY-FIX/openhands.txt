Apply only. No analysis/QA/screenshots/push/manual edits.

cd /tmp && rm -rf p && git clone -q https://github.com/mohamedamouseo-a11y/TOS-Patchs p && cd p && git checkout -q <PATCH_COMMIT>
python3 TOS-TASK-BOARD-R32-R4-WINDOW-STATE-SPECIFICITY-FIX/apply_r32_r4_production.py /var/www/TOS

Return only command output. On failure stop.
