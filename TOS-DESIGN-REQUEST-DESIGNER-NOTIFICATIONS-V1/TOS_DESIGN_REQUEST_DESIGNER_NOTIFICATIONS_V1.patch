diff --git a/backend/src/routes/tasks.routes.js b/backend/src/routes/tasks.routes.js
--- a/backend/src/routes/tasks.routes.js
+++ b/backend/src/routes/tasks.routes.js
@@ -5200,5 +5200,21 @@
     if (Object.prototype.hasOwnProperty.call(data, "estimatedHours") && data.estimatedHours !== task.estimatedHours) await logActivity(tx, { taskId: task.id, userId: req.user.id, type: "UPDATED", message: "Estimated hours changed from Design Queue", metadata: { from: task.estimatedHours, to: data.estimatedHours, source: "DESIGN_QUEUE" } });
     await logActivity(tx, { taskId: task.id, userId: req.user.id, type: "UPDATED", message: "Design Queue assignment updated", metadata: { fields: Object.keys(data), source: "DESIGN_QUEUE" } });
-    if (data.assigneeId && data.assigneeId !== designTaskAssigneeIds(task)[0]) await tx.notification.create({ data: { recipientId: data.assigneeId, actorId: req.user.id, type: "DESIGN_TASK_ASSIGNED", title: "تم إسناد طلب تصميم إليك", body: task.title, metadata: { taskId: task.id, projectId: task.projectId, source: "DESIGN_QUEUE" } } });
+    if (data.assigneeId && data.assigneeId !== designTaskAssigneeIds(task)[0]) await tx.notification.create({
+      data: {
+        recipientId: data.assigneeId,
+        actorId: req.user.id,
+        type: "DESIGN_TASK_ASSIGNED",
+        title: "تم إسناد طلب تصميم إليك",
+        body: task.title,
+        metadata: {
+          taskId: task.id,
+          projectId: task.projectId,
+          projectName: task.project?.name || null,
+          title: task.title,
+          source: "DESIGN_QUEUE",
+          url: `/design-queue?taskId=${encodeURIComponent(task.id)}`,
+        },
+      },
+    });
     if (task.designRequestedById && task.designRequestedById !== req.user.id && data.assigneeId !== designTaskAssigneeIds(task)[0]) await tx.notification.create({ data: { recipientId: task.designRequestedById, actorId: req.user.id, type: "DESIGN_REQUEST_ASSIGNED", title: "تم توزيع طلب التصميم", body: task.title, metadata: { taskId: task.id, projectId: task.projectId, assigneeId: data.assigneeId || null, source: "DESIGN_QUEUE" } } });
     return tx.task.findUnique({ where: { id: saved.id }, include: { assignee: true, designRequestedBy: true, files: { where: { deletedAt: null }, orderBy: { createdAt: "desc" } }, assignees: { include: { user: true }, orderBy: { createdAt: "asc" } }, board: true, list: true, project: { select: { id: true, name: true, type: true, status: true, stage: true, progress: true, clientName: true, client: { select: { name: true } } } } } });
@@ -5230,5 +5230,10 @@
   emitTaskToBoard(req, updated, "task:updated");
   pushTaskEventToTcrmSafe(updated, "task.updated", req.user);
   pushProjectProgressEventToTcrmSafe(updated.projectId, "project.progress.updated", req.user);
+  const previousDesignAssigneeId = designTaskAssigneeIds(task)[0] || null;
+  const assignedDesignerId = designTaskAssigneeIds(updated)[0] || null;
+  if (assignedDesignerId && assignedDesignerId !== previousDesignAssigneeId) {
+    req.app.get("io")?.to(`user:${assignedDesignerId}`).emit("design-request:assigned", { taskId: updated.id });
+  }
   res.json(safeDesignQueueTask(updated, context, context.viewer || req.user));
 }));
diff --git a/frontend/src/components/layout/Topbar.jsx b/frontend/src/components/layout/Topbar.jsx
--- a/frontend/src/components/layout/Topbar.jsx
+++ b/frontend/src/components/layout/Topbar.jsx
@@ -6,2 +6,3 @@
 import { api } from "../../lib/api";
+import { connectSocket } from "../../lib/socket";
 import { useRealtimeRefresh } from "../../lib/realtimeStateSync";
@@ -42,3 +43,12 @@
     return () => window.clearInterval(timer);
   }, [user?.id]);
+
+  useEffect(() => {
+    if (!user?.id) return undefined;
+    const socket = connectSocket();
+    const handleDesignAssignment = () => { void loadNotifications(); };
+    socket.on("design-request:assigned", handleDesignAssignment);
+    return () => socket.off("design-request:assigned", handleDesignAssignment);
+  }, [user?.id]);
+
   useRealtimeRefresh(["notifications", "employeeWork"], loadNotifications, { debounceMs: 250, minIntervalMs: 500 });
diff --git a/frontend/src/pages/DesignQueuePage.jsx b/frontend/src/pages/DesignQueuePage.jsx
--- a/frontend/src/pages/DesignQueuePage.jsx
+++ b/frontend/src/pages/DesignQueuePage.jsx
@@ -50,3 +50,13 @@
 }
 
+function isIndividualDesigner(user) {
+  const role = String(user?.role || "").toUpperCase();
+  if (["SUPER_ADMIN", "ADMIN", "MANAGER", "PROJECT_MANAGER"].includes(role)) return false;
+  return /^(design|designer|graphic design|تصميم)$/.test(String(user?.department || "").trim().toLowerCase());
+}
+
+function notificationTaskId(item) {
+  return String(item?.metadata?.taskId || "").trim();
+}
+
 function initials(value = "") {
@@ -213,3 +225,3 @@
-function TaskCard({ task, onSelect, capacityMode, tr, lang }) {
+function TaskCard({ task, onSelect, capacityMode, tr, lang, isNew = false }) {
   const overdue = task?.dueDate && !CLOSED_STATUSES.has(task.status) && new Date(task.dueDate) < new Date();
   return (
@@ -5,4 +5,7 @@
       <div className="flex items-start justify-between gap-2">
         <div className="min-w-0"><div className="line-clamp-2 text-sm font-black leading-6 text-zinc-950 dark:text-white">{task.title}</div><div className="mt-1 truncate text-[11px] font-bold text-zinc-400">{task.project?.name || tr.common.noProject}</div></div>
-        <Badge tone={PRIORITY_TONES[task.priority] || "neutral"}>{tr.priority[task.priority] || task.priority}</Badge>
+        <div className="flex shrink-0 flex-col items-end gap-1.5">
+          {isNew && <Badge tone="blue">{lang === "en" ? "New" : "جديد"}</Badge>}
+          <Badge tone={PRIORITY_TONES[task.priority] || "neutral"}>{tr.priority[task.priority] || task.priority}</Badge>
+        </div>
       </div>
@@ -240 +255 @@
-function KanbanBoard({ columns, onSelectTask, loading, capacityMode, tr, lang }) {
+function KanbanBoard({ columns, onSelectTask, loading, capacityMode, tr, lang, newTaskIds }) {
@@ -258 +273 @@
-              <div className="min-h-0 flex-1 space-y-2.5 overflow-y-auto p-2.5">{tasks.map((task) => <TaskCard key={task.id} task={task} onSelect={onSelectTask} capacityMode={capacityMode} tr={tr} lang={lang} />)}{!tasks.length && <div className="grid min-h-28 place-items-center rounded-2xl border border-dashed border-zinc-200 bg-white/70 px-4 text-center text-xs font-bold text-zinc-400 dark:border-white/10 dark:bg-white/[0.02]">{tr.queue.noColumnTasks}</div>}</div>
+              <div className="min-h-0 flex-1 space-y-2.5 overflow-y-auto p-2.5">{tasks.map((task) => <TaskCard key={task.id} task={task} onSelect={onSelectTask} capacityMode={capacityMode} tr={tr} lang={lang} isNew={newTaskIds?.has(String(task.id))} />)}{!tasks.length && <div className="grid min-h-28 place-items-center rounded-2xl border border-dashed border-zinc-200 bg-white/70 px-4 text-center text-xs font-bold text-zinc-400 dark:border-white/10 dark:bg-white/[0.02]">{tr.queue.noColumnTasks}</div>}</div>
@@ -521,6 +541,8 @@
   const { lang, isAr } = usePreferences();
   const tr = getDesignOperationsT(lang);
+  const designerMode = isIndividualDesigner(user);
   const [data, setData] = useState({ summary: {}, designers: [], tasks: [], settings: {}, permissions: {} });
   const [projectOptions, setProjectOptions] = useState(() => mergeDesignQueueProjectOptions([], projects));
-  const [filters, setFilters] = useState({ projectId: "all", status: "all", assignee: "all", lifecycle: "active" });
+  const [filters, setFilters] = useState({ projectId: "all", status: "all", assignee: designerMode && user?.id ? user.id : "all", lifecycle: "active" });
+  const [newTaskIds, setNewTaskIds] = useState(() => new Set());
   const [query, setQuery] = useState("");
@@ -590,6 +612,12 @@
-        const [payload, refreshedDetails] = await Promise.all([
+        const [payload, refreshedDetails, notifications] = await Promise.all([
           api.tasks.designQueue(nextFilters),
           openTaskId ? api.tasks.designQueueDetails(openTaskId).catch(() => null) : Promise.resolve(null),
+          api.users.notifications().catch(() => []),
         ]);
         const nextData = { summary: payload?.summary || {}, designers: payload?.designers || [], tasks: payload?.tasks || [], settings: payload?.settings || {}, permissions: payload?.permissions || {}, leadership: payload?.leadership || {} };
+        const unreadAssignedTaskIds = new Set((notifications || [])
+          .filter((item) => !item?.readAt && item?.type === "DESIGN_TASK_ASSIGNED")
+          .map(notificationTaskId)
+          .filter(Boolean));
+        setNewTaskIds(unreadAssignedTaskIds);
         setData(nextData);
@@ -620,2 +650,9 @@
-  useEffect(() => { loadQueue(); }, []);
+  useEffect(() => {
+    const requestedTaskId = typeof window !== "undefined"
+      ? String(new URLSearchParams(window.location.search).get("taskId") || "").trim()
+      : "";
+    loadQueue().then(() => {
+      if (requestedTaskId) void openTask(requestedTaskId);
+    });
+  }, []);
 
@@ -665,4 +705,14 @@
   async function openTask(taskId) {
     setSelectedTaskId(taskId); setSelectedTaskDetails(null); setDetailsOpen(true); setDetailsLoading(true);
+    setNewTaskIds((current) => {
+      const next = new Set(current);
+      next.delete(String(taskId));
+      return next;
+    });
+    void api.users.notifications()
+      .then((items) => Promise.all((items || [])
+        .filter((item) => !item?.readAt && item?.type === "DESIGN_TASK_ASSIGNED" && notificationTaskId(item) === String(taskId))
+        .map((item) => api.users.markNotificationRead(item.id).catch(() => null))))
+      .catch(() => {});
     window.requestAnimationFrame(() => window.scrollTo({ top: 0, behavior: "smooth" }));
     const base = data.tasks.find((task) => task.id === taskId);
@@ -792 +842 @@
-          <Field as="select" value={filters.assignee} onChange={(event) => updateFilters({ assignee: event.target.value })}><option value="all">{tr.queue.filters.assignees}</option><option value="unassigned">{tr.queue.stats.unassigned}</option>{data.designers.map((designer) => <option key={designer.id} value={designer.id}>{designerLabel(designer, tr.common.designer)}</option>)}</Field>
+          <Field as="select" value={filters.assignee} onChange={(event) => updateFilters({ assignee: event.target.value })}><option value="all">{tr.queue.filters.assignees}</option>{designerMode && user?.id && <option value={user.id}>{isAr ? "مهامي" : "My tasks"}</option>}<option value="unassigned">{tr.queue.stats.unassigned}</option>{data.designers.filter((designer) => !designerMode || designer.id !== user?.id).map((designer) => <option key={designer.id} value={designer.id}>{designerLabel(designer, tr.common.designer)}</option>)}</Field>
@@ -800 +850 @@
-        <div className="h-[calc(100%-65px)] min-h-0"><KanbanBoard columns={kanbanColumns} onSelectTask={openTask} loading={loading} capacityMode={settings.capacityMode} tr={tr} lang={lang} /></div>
+        <div className="h-[calc(100%-65px)] min-h-0"><KanbanBoard columns={kanbanColumns} onSelectTask={openTask} loading={loading} capacityMode={settings.capacityMode} tr={tr} lang={lang} newTaskIds={newTaskIds} /></div>
       </Card>
     </div>
   );
 }
diff --git a/frontend/src/components/layout/Sidebar.jsx b/frontend/src/components/layout/Sidebar.jsx
--- a/frontend/src/components/layout/Sidebar.jsx
+++ b/frontend/src/components/layout/Sidebar.jsx
@@ -31,2 +31,5 @@
 import { canManageUsers } from "../../lib/permissions";
+import { api } from "../../lib/api";
+import { connectSocket } from "../../lib/socket";
 import { pathForPage } from "../../lib/pageRoutes";
+import { useRealtimeRefresh } from "../../lib/realtimeStateSync";
@@ -194,4 +197,5 @@
   const [activeSettingsSection, setActiveSettingsSection] = useState(readSettingsSectionFromLocation);
   const [collapsedTooltip, setCollapsedTooltip] = useState(null);
+  const [designUnreadCount, setDesignUnreadCount] = useState(0);
 
   const { lang } = usePreferences();
@@ -208,3 +212,32 @@
   const settingsSubnavItems = getSettingsSubnavItemsForUser(user, lang);
 
+  async function loadDesignUnreadCount() {
+    if (!user?.id || !getNavItemAccess("designQueue", user)) {
+      setDesignUnreadCount(0);
+      return;
+    }
+    try {
+      const notifications = await api.users.notifications();
+      setDesignUnreadCount((notifications || []).filter((item) => !item?.readAt && item?.type === "DESIGN_TASK_ASSIGNED").length);
+    } catch {
+      setDesignUnreadCount(0);
+    }
+  }
+
   useEffect(() => {
+    loadDesignUnreadCount();
+    const timer = window.setInterval(loadDesignUnreadCount, 60000);
+    return () => window.clearInterval(timer);
+  }, [user?.id]);
+
+  useEffect(() => {
+    if (!user?.id) return undefined;
+    const socket = connectSocket();
+    const handleDesignAssignment = () => { void loadDesignUnreadCount(); };
+    socket.on("design-request:assigned", handleDesignAssignment);
+    return () => socket.off("design-request:assigned", handleDesignAssignment);
+  }, [user?.id]);
+
+  useRealtimeRefresh(["notifications", "tasks", "employeeWork"], loadDesignUnreadCount, { debounceMs: 250, minIntervalMs: 500 });
+
+  useEffect(() => {
@@ -408,2 +441,6 @@
                   <span className="truncate">{subItem.label}</span>
-                  <span className={cn("ms-auto h-1.5 w-1.5 rounded-full", subSelected ? "bg-amber-500" : "bg-slate-300")} />
+                  {subItem.id === "designQueue" && designUnreadCount > 0 ? (
+                    <span className="ms-auto grid h-5 min-w-5 place-items-center rounded-full bg-red-600 px-1 text-[10px] font-black text-white">{designUnreadCount > 9 ? "9+" : designUnreadCount}</span>
+                  ) : (
+                    <span className={cn("ms-auto h-1.5 w-1.5 rounded-full", subSelected ? "bg-amber-500" : "bg-slate-300")} />
+                  )}
                 </a>
               );
