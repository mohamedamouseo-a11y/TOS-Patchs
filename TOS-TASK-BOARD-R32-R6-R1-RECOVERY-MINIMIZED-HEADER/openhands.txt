Apply recovery only. No analysis/QA/screenshots/push/manual edits.

cd /tmp && rm -rf p && git clone -q https://github.com/mohamedamouseo-a11y/TOS-Patchs p && cd p && git checkout -q <PATCH_COMMIT>
python3 TOS-TASK-BOARD-R32-R6-R1-RECOVERY-MINIMIZED-HEADER/apply_r32_r6_r1_production.py /var/www/TOS

Return only command output. On failure stop.