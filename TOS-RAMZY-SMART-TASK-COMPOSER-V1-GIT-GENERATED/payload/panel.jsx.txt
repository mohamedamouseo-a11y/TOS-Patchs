            {smartTask ? (
              <div className="ramzy-smart-task-panel" role="region" aria-label={isEnglish ? "Smart task creator" : "إنشاء مهمة ذكي"}>
                <div className="ramzy-smart-task-head">
                  <div><strong>{isEnglish ? "Smart task creator" : "إنشاء مهمة ذكي"}</strong><small>{isEnglish ? `Step ${smartTaskStepNumber()} of 5` : `الخطوة ${smartTaskStepNumber()} من 5`}</small></div>
                  <button type="button" className="ramzy-smart-task-close" onClick={cancelSmartTask} aria-label={isEnglish ? "Cancel smart task" : "إلغاء إنشاء المهمة"}><X size={14} /></button>
                </div>
                <div className="ramzy-smart-task-progress" aria-hidden="true">{[1, 2, 3, 4, 5].map((step) => <i key={step} className={step <= smartTaskStepNumber() ? "is-done" : ""} />)}</div>
                {smartTask.step === "TITLE" && <p className="ramzy-smart-task-copy">{isEnglish ? "Type only the task title below, then press Enter. Ramzy will guide the rest with quick choices." : "اكتب اسم التاسك فقط تحت واضغط Enter. رمزي هيكمّل معاك بالباقي كاختيارات سريعة."}</p>}
                {smartTask.step === "PROJECT" && <><p className="ramzy-smart-task-copy">{isEnglish ? "Choose the project:" : "اختار المشروع:"}</p>{smartTaskOptions.loading && <div className="ramzy-smart-task-loading">{isEnglish ? "Loading projects..." : "جاري تحميل المشاريع..."}</div>}{smartTaskOptions.error && <div className="ramzy-smart-task-error">{smartTaskOptions.error}</div>}<div className="ramzy-smart-task-options">{smartTaskOptions.projects.map((item) => <button type="button" className="ramzy-smart-task-option" key={smartTaskOptionId(item)} onClick={() => chooseSmartTaskProject(item)}>{smartTaskOptionName(item)}</button>)}<button type="button" className="ramzy-smart-task-option is-primary" onClick={() => chooseSmartTaskProject("AUTO")}>{isEnglish ? "✨ Let Ramzy suggest" : "✨ خلي رمزي يقترح"}</button></div></>}
                {smartTask.step === "DUE" && <><p className="ramzy-smart-task-copy">{isEnglish ? "When should it be due?" : "الـ Due Date إمتى؟"}</p><div className="ramzy-smart-task-options">{["TODAY", "TOMORROW", "IN_2_DAYS", "IN_1_WEEK", "NONE"].map((kind) => <button type="button" className="ramzy-smart-task-option" key={kind} onClick={() => chooseSmartTaskDue(kind)}>{smartTaskDueLabel(kind, isEnglish)}</button>)}</div></>}
                {smartTask.step === "PRIORITY" && <><p className="ramzy-smart-task-copy">{isEnglish ? "Choose priority:" : "اختار الأولوية:"}</p><div className="ramzy-smart-task-options">{["HIGH", "MEDIUM", "LOW", "AUTO"].map((priority) => <button type="button" className={`ramzy-smart-task-option${priority === "AUTO" ? " is-primary" : ""}`} key={priority} onClick={() => chooseSmartTaskPriority(priority)}>{smartTaskPriorityLabel(priority, isEnglish)}</button>)}</div></>}
                {smartTask.step === "ASSIGNEE" && <><p className="ramzy-smart-task-copy">{isEnglish ? "Who should own it?" : "مين ينفذ التاسك؟"}</p><div className="ramzy-smart-task-options"><button type="button" className="ramzy-smart-task-option is-primary" onClick={() => chooseSmartTaskAssignee("AUTO")}>{isEnglish ? "✨ Let Ramzy suggest" : "✨ خلي رمزي يقترح الأنسب"}</button><button type="button" className="ramzy-smart-task-option" onClick={() => chooseSmartTaskAssignee("NONE")}>{isEnglish ? "Unassigned" : "بدون تحديد"}</button>{smartTaskOptions.users.map((item) => <button type="button" className="ramzy-smart-task-option" key={smartTaskOptionId(item)} onClick={() => chooseSmartTaskAssignee(item)}>{smartTaskOptionName(item)}</button>)}</div></>}
                {smartTask.step === "REVIEW" && <><p className="ramzy-smart-task-copy">{isEnglish ? "Ready. Review the quick summary, then hand it to Ramzy." : "تمام. راجع الملخص السريع وبعدها سلّمها لرمزي."}</p><div className="ramzy-smart-task-summary"><span><b>{isEnglish ? "Task" : "التاسك"}</b>{smartTask.title}</span><span><b>{isEnglish ? "Project" : "المشروع"}</b>{smartTask.project?.name || "-"}</span><span><b>{isEnglish ? "Due" : "الموعد"}</b>{smartTaskDueLabel(smartTask.dueKind, isEnglish)}</span><span><b>{isEnglish ? "Priority" : "الأولوية"}</b>{smartTaskPriorityLabel(smartTask.priority, isEnglish)}</span><span><b>{isEnglish ? "Assignee" : "المنفذ"}</b>{smartTask.assignee?.name || "-"}</span></div><div className="ramzy-smart-task-actions"><button type="button" onClick={() => setSmartTask((current) => ({ ...current, step: "PROJECT" }))}>{isEnglish ? "Edit choices" : "تعديل الاختيارات"}</button><button type="button" className="is-primary" onClick={submitSmartTask}>{isEnglish ? "Send to Ramzy" : "سلّمها لرمزي"}</button></div></>}
              </div>
            ) : null}
